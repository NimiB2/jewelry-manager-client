import { Section } from './Section'

// Placeholder — the previous generic stage/term builder wasn't clear enough
// to actually use. Waiting for the exact formula spec before rebuilding this.
export function PricingFormulaEditor() {
  return (
    <Section title="נוסחת תמחור">
      <p style={{ fontSize: 14, color: 'var(--text-muted)' }}>
        בקרוב — נבנה כאן את הנוסחה המדויקת לפי מה שתשלחי.
      </p>
    </Section>
  )
}
